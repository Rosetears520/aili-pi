## Why

The accepted AILI Compact release contract names `v0.1.14` as a separately published P0 predecessor, but repository history contains no `v0.1.14` tag, commit, package identity, or recoverable P0-only candidate. The current tree contains the P0 recovery behavior, the complete lifecycle redesign, and the emergency-checkpoint correction together. Creating or relabeling an artifact as `v0.1.14` would fabricate release history and still would not provide trustworthy rollback evidence.

The release path must preserve every P0 safety invariant while grounding migration, rollback, package identity, and release evidence in versions that actually exist. The user accepted on 2026-08-02 that the nonexistent `v0.1.14` will not be manufactured, the merged capability line will target `v0.2.0`, and the latest real published `0.1.x` predecessor—expected from current local Git evidence to be `v0.1.16`—will be verified before it is used for rollback evidence. On 2026-08-03 the user also decided that `@narumitw/pi-lsp` and `pi-markdown-preview` are not part of the intended `0.2.0` product surface and must be removed rather than merely unbundled. On 2026-08-04, after two exact OpenAI captures proved the real transport/order and parent-to-child lifecycle boundary but did not naturally induce pressure, context overflow, sandbox marker work, or provider-authored tier transactions, the user selected a deterministic production-entry verification strategy for those failure/side-effect paths instead of further billable failure induction. BUILD then exposed an official-Pi compatibility defect: Pi represents the root Session entry with `parentId: null`, while the Compact branch index rejected every defined non-string parent. The user selected a bounded production-path repair rather than weakening the accepted evidence class. Controlled production subsequently exposed safe-range and promotion defects: omitted AILI protocol created hidden source gaps, and strict raw-ordinal block adjacency made every separately created T1 ineligible for T2 promotion. The user selected source-ordinal-aware T1 range splitting plus narrowly AILI-protocol-transparent block promotion rather than weakened source contiguity, protocol summarization, or artificial fixtures. A later release review established that the partial gap-proof implementation still has divergent raw-message ordinal domains, name-only protocol ownership, mutable replay source references, and repeated full-history replay. The user selected a complete contract hardening revision rather than accepting those limitations or extending the timeout.

## What Changes

- Reclassify `fix-aili-compact-recovery-deadlock` as the mandatory P0 behavioral predecessor rather than a claim that `v0.1.14` was independently published.
- Keep `@rosetears/aili-pi@0.2.0` as the exact target for the merged P0, lifecycle-redesign, and emergency-checkpoint implementation.
- Resolve the rollback predecessor from fresh Git-remote and npm-registry evidence under separate network approval. The expected value is `0.1.16`; mismatch stops version-sensitive work instead of being guessed around.
- Replace the impossible separately-installed-`v0.1.14` migration row with a copied-session and installed-package rollback rehearsal against that verified predecessor.
- Separate the current package version from the historical `AGPL-3.0-or-later` disposition start at `0.1.13`; a `0.2.0` candidate must not rewrite license history.
- Repair the remaining stable-release blockers through their owners: current package/generated identity, the `i-have-adhd` Pi-owned presentation adapter, stale Persistent Agent live verification, and candidate-bound AILI Compact evidence.
- Preserve fail-closed release validation. Missing, stale, wrong-version, wrong-provider, unsanitized, or wrong-evidence-class evidence remains `NON_PASS`; a real transport result cannot substitute for a controlled failure-path test, and a controlled failure-path test cannot fabricate a real provider transport result.
- Require one fresh representative real-provider/Pi boundary run using an already available configured provider. It proves transport, provider protocol acceptance, controlled extension order, and a real parent-to-persistent-child lifecycle. Deterministic production-entry `AgentSession` tests own suffix/non-persistence, overflow/checkpoint/retry/later-work, process-owned child sandbox work, and lifecycle/tiering behavior. OpenAI/Anthropic/Google Gemini serializer and protocol compatibility remains deterministic and offline. Stable release does not require obtaining Anthropic or Google Gemini credentials or running a complete live matrix for every provider family.
- Treat missing provider cache telemetry as `Unverified` optimization evidence that preserves conservative behavior and does not block compression or stable publication.
- Remove `@narumitw/pi-lsp` and `pi-markdown-preview` from production registration, dependencies, bundle metadata, lock state, docs, provenance/SBOM, generated capability evidence, and public tool/command expectations. Keep `pi-cache-optimizer` as the remaining selected cache integration.
- Require package/runtime negatives proving that `lsp_diagnostics`, `lsp_fix`, `/lsp`, `/preview*`, and `preview_export` are not shipped or registered by `0.2.0`; users may install those upstream Pi extensions separately when wanted.
- Accept official Pi's `parentId: null` only for the actual root Session entry in cold build or the first append to an empty index. Preserve fail-closed rejection of null or malformed parents everywhere else and add exact official-Pi AgentSession regressions.
- Exercise controlled Compact tool calls with a disposable test-only permission configuration that preauthorizes only `aili_compact_status` and `aili_compact`, binds the real extension lifecycle, and leaves shipped permission defaults and headless `ask → deny` behavior unchanged.
- Preserve AILI protocol exclusion from public refs and recent-tail aging, but split every advertised safe source range at an effective-provider-ordinal discontinuity so persisted omitted protocol remains a hard source boundary and every recommendation is executable under exact mutation validation.
- Define T2/T3 promotion adjacency over semantic leaves: an ordinal gap between child blocks is transparent only when every omitted raw slot is proven to be complete, closed-schema, handler-attested AILI-owned `aili_compact_status`/`aili_compact` planning protocol. Ordinary messages, third-party tools, malformed or permission-denied protocol, unknown ordinals, and mixed gaps remain fail-closed.
- Make every `type:"message"` Session entry consume one raw epoch ordinal, even when its message body is missing, `null`, scalar, array, duplicate-ID, or otherwise malformed. Valid public references and semantic leaves use stricter predicates, but no stricter predicate may renumber the raw domain.
- Replace name-only transparent-protocol recognition with a closed replayable AILI planning attestation bound to the exact call, result, session, epoch, extension implementation identity, outcome, result digest, and—when successful compacting—the matching v3 transaction ID and transaction digest. This is source binding, not secret authentication: no HMAC, credential, or unverifiable external service is introduced.
- Keep raw branch-message intervals authoritative through immutable, source-digested epoch snapshots and bounded versioned gap proofs. Creation and replay independently resolve the same immutable branch slice, verify endpoints/count/digest/attestation/classification, and rerun the strict classifier; a transaction claim alone never proves transparency.
- Bind replay seeds and catalog identity to the source prefix and structural block content, then replace repeated full-history status/replay work with one revision-scoped promotion-gap index and bounded raw-range verification.
- Retire the fixed T1 → T2 → T3 → restill hierarchy, its exact 50-transaction fixture, and tier-specific immediate-child economics as the primary compaction/recovery mechanism. New model-authored mutations create source-backed active blocks or atomically replace explicitly selected active blocks; they are bounded by exact source/proof, quality, and current request context rather than inherited tier/count/age/source-floor gates. Existing v3 tiered records remain readable for rollback and source proof, but no new write depends on their topology.
- Adopt ACP lifecycle behavior and MiMoCode recovery behavior as separate AILI-owned designs: the current agent continues to author compact summaries through Compact tools; source-backed active blocks are retrieved only through explicit search/recap/decompression; and provider projection is protected recent raw content plus bounded descriptors, never automatic historical replay. Independently, a MiMo-style dynamic checkpoint writer prepares rebuild state from the effective context window; recovery at the reserved safe budget or provider overflow rebuilds from a current checkpoint first and invokes bounded Pi-native compaction only when that rebuild is unavailable.
- Keep the authoritative source-proof ledger separate from the provider-facing frontier. Omission from the frontier never deletes, weakens, renumbers, or makes source/proofs unavailable to planner, mutation, replay, BranchIndex, search, or decompression. An unavailable or ambiguous context budget must fail closed for automatic expansion while preserving the existing exact-source fallback and diagnostics.
- Require rollback, package dry-run, full regression, and human acceptance of the evidence boundary, quality limitations, and remaining `Unverified` claims before stable publication. A provider-authored long-lifecycle review candidate is not required when deterministic production-entry evidence owns that claim.

## Capabilities

### New Capabilities

- `aili-compact-release-lineage`: truthful predecessor resolution, P0 behavioral inheritance, rollback rehearsal, candidate identity, and release-evidence binding for `v0.2.0`.

### Modified Capabilities

- `aili-compact-branch-index`: accept official Pi's null parent sentinel only for the actual Session root while preserving strict later ancestry validation; retain immutable proof-relevant raw projections rather than mutable Session message references, bind replay seeds to a source prefix, and expose bounded raw epoch/gap access with truthful traversal counters.
- `aili-compact-safe-planning`: keep omitted AILI protocol out of source refs and tail aging while splitting recommendations at its effective-provider-ordinal gaps.
- `aili-compact-tiered-lifecycle`: migrate new writes from fixed T1/T2/T3/restill promotion to source-backed active blocks with explicit bounded recap/search/decompression and atomic selected-block replacement; preserve exact raw-source proof and v3 read compatibility.
- `aili-compact-checkpoint-recovery`: preserve P0 recovery as a behavioral prerequisite without asserting a nonexistent `v0.1.14` publication, and replace fixed pressure stages with MiMo-style dynamic checkpoint/rebuild-first recovery.
- `reversible-context-compression`: bind migration and rollback evidence to the verified real published predecessor and the exact `0.2.0` candidate.

## Impact

- Contract and validation: active AILI Compact release docs, migration rows, release-evidence validator, sequence checks, and focused tests.
- Runtime metadata: package/candidate version ownership and license-since ownership in `src/runtime/registry.ts` and direct consumers.
- Compatibility: a Pi-owned `i-have-adhd` presentation adapter plus generated compatibility/provenance evidence; no shared skill-body edit.
- Candidate metadata: `package.json`, lock root, SBOM, notices/provenance, release artifacts, and package dry-run output only under their applicable operation approvals.
- Live evidence: one current configured-provider official-Pi boundary for transport, provider protocol acceptance, controlled extension order, and parent-to-persistent-child lifecycle. The two 2026-08-04 OpenAI captures remain honest limitation evidence for real pressure/overflow/sandbox-marker/tier observations and are not relabeled as those claims.
- Controlled production evidence: suffix/non-persistence, overflow/checkpoint/original-request retry/later work, process-owned child sandbox marker work, active-block projection, and dynamic checkpoint/rebuild run through official Pi production `AgentSession`/entry seams with deterministic providers and exact negative cases.
- Production compatibility: strictly normalize official Pi's null root-parent sentinel at the branch-index boundary; test-only exact Compact tool permission preauthorization must not alter shipped permission policy; safe planning must not advertise ranges that bridge omitted persisted AILI protocol; promotion may bridge only fully attested AILI-owned planning protocol from an immutable raw epoch snapshot.
- Replay performance: one source revision builds the protocol/gap index at most once; status classifies all promotion pairs from that index; T1/control/decompress replay does not materialize pre-tip raw history; block-parent verification reads only the bounded proof range; pure replay health audit is source-digest/version scoped and cacheable.
- Provider projection: an AILI-owned ACP-style frontier exposes recent protected raw content and at most 32 active block descriptors by default. Full summaries are not automatically appended for every active block; explicit recap/decompression resolves only the requested 1–16 current block references and rejects expansion that cannot fit the current model-context budget. The source-proof ledger stays complete and independently replayable.
- Dynamic-compaction evidence: each safe-window category must use the exact MiMo default checkpoint ladder, preserve the active-block/source-proof boundary across a checkpoint rebuild, and use the bounded Pi-native fallback only when a checkpoint is unavailable, stale, or failed. The prior fixed hierarchy is migration/read-compatibility evidence only, not a release gate.
- Offline compatibility: deterministic provider serializer/protocol fixtures cover OpenAI, Anthropic, and Google Gemini without provider authentication or requests.
- Package surface: remove the two optional upstream integrations through their runtime/package/generated owners; retain normal independent installation as the user opt-in path rather than an AILI compatibility shim.
- Non-goals: no dependency addition, no replacement LSP/preview implementation, no fabricated `v0.1.14`, no Pi fork, no broad Compact permission exemption, no headless approval injector, no filtered-ordinal mutation semantics, no protected fixture separator that conceals a planning defect, no timeout increase as a performance substitute, no secret/HMAC ownership scheme, no Anthropic/Google Gemini credential requirement, no DCP-style historical-summary/protected-output expansion, no copied ACP/MiMoCode code/hooks/prompts/configuration/state/dependencies, no new user configuration surface in this delta, and no implicit network, credential, Git, tag, publish, or release authority.

## Review-driven remediation proposal — 2026-08-10

### Why

The bounded complexity review found two current contract violations and two stale release-evidence paths. It also found a high-cost oracle matrix that repeatedly proves the same implementation paths without covering a deliberate child-count × protocol-kind matrix. The required source-proof, closed attestation, immutable replay, and independent planner/pure-replay/BranchIndex checks remain justified: they defend separate persisted-history and provider-facing trust boundaries and are not candidates for removal.

### Proposed changes

- Anchor `validateAiliCompactReleaseEvidence` to the active `reconcile-aili-compact-release-lineage` contract. A missing retired OpenSpec directory must yield a bounded `NON_PASS`, never an empty error list.
- Preserve mixed legacy/v3 read compatibility while ordering their unified provider-frontier catalog by effective semantic-leaf order before assigning public block references or applying the 32-descriptor cap.
- Complete the already accepted frontier identity work in task 2.15: bind recap selection invalidation to verified branch, epoch, source revision, proof revision, descriptor identity, model context/reserve, and configuration. This is required hardening, not a simplification target.
- Replace the obsolete tiered human-review candidate wrapper with one current, verifiable review-artifact schema that preserves the required human evidence-boundary review without requiring provider-authored tier transactions.
- Keep the independent promotion oracle, but replace its correlated 0–256 exhaustive loop with boundary lengths (`0`, `1`, `2`, `255`, `256`, `257`) crossed with the minimum and maximum child cardinalities (`2`, `16`) and every protocol classification. Planner, direct mutation, pure replay, cold BranchIndex replay, and append BranchIndex replay must still agree for every table row; focused active-block tests retain ordinary two-to-sixteen behavior coverage.
- Audit the old `projectIndexedProviderMessages` full-history projector before removing it. Remove it and its private-only tests only if no supported external consumer or current production entry depends on it; otherwise document it as a compatibility surface with an actual caller.

### Boundaries

- Do not weaken raw-ordinal accounting, bounded proof limits, closed handler attestation, immutable source snapshots, source-bound replay, or independent replay validation.
- Do not remove human review from the candidate gate; only retire its stale tier-specific wrapper.
- Do not add dependencies, user configuration, external access, provider calls, installation, Git, or publication work.
- This is a proposed material correction to validation and verification strategy. It requires coherent design/task/test-plan updates, strict validation, and fresh explicit final test-plan acceptance before affected BUILD work resumes.

### Direction acceptance

On 2026-08-10, the user accepted this remediation direction for DEFINE write-back. This authorizes only the coherent OpenSpec contract revision; it is not final `test-plan.md` acceptance or authority to resume affected BUILD work.
