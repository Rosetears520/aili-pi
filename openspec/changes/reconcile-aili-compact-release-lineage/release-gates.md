# Release Gates: reconcile-aili-compact-release-lineage

Target: `@rosetears/aili-pi@0.2.0`, defined as the user-selected complete current working tree. This document authorizes no network, credential, installation, version, lockfile, Git, tag, publish, or release operation.

## Blocking gates

- [ ] Fresh Git/npm evidence agrees on the real `0.1.x` predecessor; expected `0.1.16`.
- [ ] Active contracts and validators contain no fabricated separately-installed `v0.1.14` requirement.
- [ ] P0 recovery, lifecycle redesign, and emergency-checkpoint regression matrices pass on the final tree.
- [ ] Exact package/lock/SBOM/runtime identity is `0.2.0`; license history remains anchored at `0.1.13`.
- [x] `@narumitw/pi-lsp` and `pi-markdown-preview`, their native registrations, public tools/commands, active provenance/capability claims, and unreachable bundled closure are absent; `pi-cache-optimizer` and retained integrations still load.
- [ ] `i-have-adhd` has a tested Pi-owned adapter and generated compatibility evidence is not blocked.
- [ ] The fresh real-provider boundary binds current hashes and passes official-Pi transport/protocol acceptance, controlled extension order, exact parent task arguments, zero parent Bash calls, and completed persistent child lifecycle.
- [ ] Official-Pi controlled-production tests pass transient suffix/non-persistence, context-error overflow/checkpoint/original-request retry/later work, process-owned child sandbox marker work, tierless active-block frontier/replacement, and MiMo recovery without direct event injection or manual promotion.
- [ ] Official Pi root `parentId: null` is accepted only at the true root; later null/malformed parents remain fail-closed, and real AgentSession evidence records a healthy index plus provider-message alignment.
- [ ] Controlled Compact tool calls use exact disposable test-only preauthorization and the real extension lifecycle; shipped permission defaults and headless `ask → deny` remain unchanged.
- [ ] Current and indexed safe planning split recommendations at every effective-source-ordinal gap left by omitted persisted AILI protocol; AILI protocol remains excluded from public refs/tail aging and exact mutation contiguity remains fail-closed.
- [ ] Every `type:"message"` entry uses the one raw epoch ordinal domain; malformed/non-record slots remain non-semantic but cannot disappear from a promotion gap or make pure/index ordinal divergence possible, while a duplicate message entry ID rejects the entire epoch consistently.
- [ ] Lifecycle status and mutation validation share one revision-scoped promotion-gap classifier that bridges only successful closed status, successful transaction-bound compact, or closed handler-attested compact rejection; rejected status, permission-denied, ordinary, third-party, malformed, unknown, case/whitespace/Unicode-variant, and mixed gaps remain fail-closed, and parent leaf digests/counts exclude protocol.
- [ ] Every non-empty transparent promotion gap has a bounded (maximum 256 slots) versioned raw-slice-and-attestation proof including `sourceSnapshotDigest`, independently revalidated by planner, pure replay, and BranchIndex replay against the same immutable source-bound snapshot; parent raw hull may exceed semantic leaf count, while forged/unknown/oversized proofs, stale/mutable source, seed mismatch, and replay disagreement fail closed.
- [ ] Source-bound replay seed and structural catalog identity reject altered source/proof/transaction content, including a source snapshot digest change, cross-branch/epoch proof reuse, and structurally valid but source-invalid archived state.
- [ ] Production index/status/replay uses one revision-scoped gap index, emits truthful traversal counters, confines proof replay to at most 256 raw slots, and grows a controlled active-block ledger beyond the 32-descriptor frontier without omitted-history replay. Explicit two-to-sixteen block composition remains source-bound and atomic. Timeout, killed worker, missing artifact, artifact hash mismatch, or a final assertion failure with an adoptable PASS artifact is non-PASS.
- [ ] New compression writes have no T1/T2/T3/restill rank, fixed hierarchy, tier age/source-floor, or tier-economics dependency. Legacy tiered records remain readable for rollback/source proof. Semantic summaries use a 15,000-character default target and synchronized 18,000-character runtime/configuration/v3/quality hard ceiling; every applicable owner accepts 18,000 and rejects 18,001.
- [ ] The AILI-owned ACP-style provider frontier remains distinct from the complete source-proof ledger: protected recent raw plus at most 32 descriptors are projected by default; only explicit current 1–16 recap/decompression selections that fit the model request budget add full summaries; selected-block replacement updates descriptors atomically; omitted ledger source remains exactly searchable/decompressible; unknown/stale/over-budget expansion fails closed without unbounded-history fallback; and all frontier counters are truthful.
- [ ] MiMo dynamic recovery derives Pi's combined-window safe budget from observed context plus separate 20K-capped output/recovery reserves and a 13K checkpoint ceiling. Its resolved ladders are exact by window range, only one writer runs per session, checkpoints bind current source/session/branch/epoch atomically, a successful rebuild resets thresholds, and the safe-budget/overflow path rebuilds through `context` before exactly one bounded native fallback. Double failure blocks the oversized ordinary request; a fixed 70/80/90 recovery ladder is absent.
- [ ] The slow brute-force raw-slot oracle used for differential tests does not import, call, wrap, or share the production classifier, `PromotionGapIndexV1`, or indexed range verifier.
- [ ] Real predecessor migration/rollback and copied-session continued work pass.
- [ ] OpenAI, Anthropic, and Google Gemini deterministic offline serializer/protocol compatibility passes; Anthropic/Google Gemini credentials and complete family-specific live matrices are not required.
- [ ] Missing or zero provider cache telemetry produces no cache-hit claim, remains bounded `Unverified`, and does not block release.
- [ ] The representative-provider limitation artifacts remain sanitized and truthful: absent natural pressure/overflow/sandbox-marker/active-block observations are bounded `Unverified`, not relabeled PASS and not treated as substitutes for controlled-production evidence.
- [ ] Candidate-bound migration, performance, fake-provider, real-boundary, controlled-production, provenance, sanitizer, and release index artifacts pass.
- [ ] Focused checks, typecheck, generated/compatibility/package/provenance, OpenSpec sequence, full test suite, release validator, diff check, and package dry-run pass.
- [ ] Human review accepts package contents, the real-vs-controlled evidence boundary, preserved live limitations, recovery, migration/rollback, cache/performance claims, and bounded unknowns; no provider-authored semantic candidate is required.

## Publication boundary

Passing this checklist establishes a verified local candidate only. Commit, push, tag, npm publish, and GitHub release remain distinct operations requiring exact approval.
