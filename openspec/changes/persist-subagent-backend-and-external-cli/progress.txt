BUILD started after user accepted test-plan.md and authorized implementation in the current main worktree despite existing mixed changes.

P1 persistent global backend command completed: `/aili-agent-backend global herdr|managed|clear` writes the global backend preference atomically, preserves valid `herdr` settings, updates the current session's future-Agent override, and retains session-only `s|h|m`. Verification passed: `npx vitest run tests/unit/persistent-agent-backends.test.ts` (23/23) and `npm run typecheck`.

P0 read-only Herdr discovery: `herdr agent start` supports direct `claude`, `codex`, `gemini`, `opencode`, `grok`, and other recognized agent kinds, while normal tab/pane allocation supports same-tab splits. User selected the persistent CUI lifecycle contract: after this prompt demonstrably enters work, return to the input-ready `idle|done` state settles the Turn; pre-existing idle, `blocked`, `unknown`, pane text, and focus do not. No agent, pane, vendor CLI, installation, login, or network action was started.

Direct external CUI path implemented with fake fixtures: exact user-authorized CLI starts as Herdr `claude|codex|gemini|opencode|grok` rather than a Pi child, reuses normal panes, requires post-prompt working then idle/done, returns bounded redacted `agent.read` output, records `external-cli` driver evidence, and never replays a lost prompt. Read-only installed Herdr schema inspection confirmed `agent.prompt {target,text,wait?}`, `agent.read {target,source,...}`, and direct Agent kinds; no live Agent was started.

Verified: focused backend/external CLI/storage/sub tests 93/93; focused production/runtime/permission/interaction tests 15/15; combined final focused matrix 109/109; `npm run typecheck`; strict OpenSpec; adapter evidence verify; extension/package/i-have-adhd rerun 7/7; task-scoped diff-check. Full `npm test` reached 894 passed / 2 skipped with two concurrent-suite failures (one package-load 5s timeout and one stale running→parked test race); both failing files passed together immediately in isolated rerun (7/7), so the full parallel suite is not reported as PASS.

Residual blocker: Herdr exposes a blocked lifecycle state but no bounded operation packet. The implementation does not open a user dialog and safely denies blocked external confirmations. A verified vendor YOLO flag is enabled only when the active Parent permission mode is already YOLO. Policy-bounded auto-confirmation of an ordinary non-YOLO vendor dialog remains unimplemented because its requested operation cannot be proven; inventing authorization would violate the accepted boundary.

Real Agy CLI smoke completed under exact user authorization. Initial attempt reused a pane created before the `.bashrc` GUI-function fix and immediately exited; that task-owned pane was closed. A fresh same-tab sibling pane then started Linux Agy CLI 1.1.22 as Herdr kind `agy` with verified `--dangerously-skip-permissions`, accepted a no-tool/no-file prompt, returned the exact expected marker, and Herdr reported input-ready `idle`; the task-owned pane was closed. No installation, login, file/tool mutation, commit, or push occurred. This proves the current Agy/Herdr CUI start-prompt-idle path, not the post-reload public `sub cli: agy-cli` schema path; another `/reload` is required before that public path can be invoked in this Parent session. Other real CUI products remain unverified.
