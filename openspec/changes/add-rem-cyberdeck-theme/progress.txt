BUILD GOAL
goal_id: build-add-rem-cyberdeck-theme-20260723
change: add-rem-cyberdeck-theme
repository_root: /home/rosetears/code/aili-pi
scope: Implement the accepted Rem Cyberdeck Theme/TUI contract: Theme JSON, supplied Unicode/Braille header, working surface, footer, editor chrome, and default-on fail-safe fixed-bottom editor. Do not fork/replace Pi, copy Sakura Zentui, add dependencies, or implement quota polling.
permission_policy: User accepted final test plan and resumed BUILD. Dependency/lockfile, external writes, Git commit/push, publish, and release remain separately gated.

QUEUE
P1 Theme and public UI surfaces (tasks 2.1-2.4): pending. Owner ROSE. Evidence: schema/discovery, focused UI lifecycle/footer tests.
P2 Editor and fixed-bottom compatibility (tasks 3.1-3.4): pending after P1. Evidence: capability rejection, transactional patch/rollback, terminal restoration tests and manual Linux TUI checks.
P3 Docs, provenance, and final verification (tasks 4.1-4.3): pending after P1/P2. Evidence: documentation/provenance inspection, changed-scope checks, package dry-run, strict validation, manual matrix.

CHECKPOINT 2026-07-23
objective: Accept the new Rem Cyberdeck Theme test plan and resume BUILD.
acceptance: User explicitly replied “接受 test-plan.md 并恢复 BUILD”.
rehydrated: AGENTS.md; exact handoff; new proposal/design/tasks/spec/test-plan; current Package/runtime/tests; locally inspected Sakura reference at 165a1f8011a12a58a6409b56b8a6c0416cd9b589; local Pi Theme/Extension/TUI documentation.
git: branch build/create-aili-pi-distribution at ae210c04e0872cae073dc3ff8da984a839f3810d; untracked graphify-out and this change directory.
verification: `openspec validate add-rem-cyberdeck-theme --strict` passed before acceptance write; refresh required after this acceptance record.
next_action: Implement P1 as a complete public-API Theme/header/working/footer slice; stop before private fixed-editor patching if its capability contract or source-reuse boundary changes.

P1 ASSET UPDATE 2026-07-23
[KNOWN|USER] The user replaced `src/runtime/rem-head.txt` with the preferred Rem Unicode/Braille art.
[COMPUTED] The contract snapshot at `assets/rem-head.txt` was synchronized byte-for-byte to the runtime asset; SHA-256 `57d1d232e16dd96cc421f1c8e2d6bfc7a3d1f4c03f8d5dc260181d9ec904865c`.
[COMPUTED] The user selected four Package Extensions: AILI plus direct MIT-derived Sakura header, matrix, and Zentui extensions. The copied header loads the supplied Rem asset; palette values use the Rem palette; Zentui contains the fixed-editor fail-safe implementation.
[COMPUTED] NodeNext compatibility adaptations add explicit relative `.js` specifiers and change the Matrix lifecycle listener to Pi's `session_before_switch` event. Package tests declare all four Extensions.
[COMPUTED] Provenance now records exact Sakura revision `165a1f8011a12a58a6409b56b8a6c0416cd9b589`; its LICENSE and NOTICE are packaged under `licenses/` and `notices/`; README documents the fixed editor and its terminal interaction boundary.
[FRAME] Per user instruction, no further automated verification is run until the implementation is presented for user-led testing. Required next evidence: provenance generation/validation, package dry-run, full test suite, strict OpenSpec validation, and manual Linux Pi TUI matrix.
