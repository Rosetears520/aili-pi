## 1. Readiness

- [x] 1.1 Accept the final `test-plan.md` before implementation.

## 2. Quota display repair

- [x] 2.1 Default `pi-cache-stats` to `off` without changing cache optimizer behavior.
- [x] 2.2 Give `pi-quota-status` deterministic priority and select one canonical weekly segment: explicit `Wk` first, legacy-mislabeled `5h` only as fallback, always rendered as `codex`.
- [x] 2.3 Add coexistence, explicit-weekly preference, single-segment, legacy fallback, unknown-status, and reset-preservation unit coverage.

## 3. Sakura visual and Matrix responsive repair

- [x] 3.1 Add a deterministic Matrix regression fixture proving ordinary-width parity, ultra-wide prefix truncation, glyph/line visible width, and bounded work.
- [x] 3.2 Replace only overflowing first-96 selection with deterministic full-width sampling; retain ordinary sparse timing and default density.
- [x] 3.3 Restore pinned Sakura reasoning-gradient stops/fallback while retaining required Pi compatibility adaptations.
- [x] 3.4 Add exact source-value regression coverage and document dark-theme/density controls.

## 4. Subagent Agent label

- [x] 4.1 Prepend a sanitized, bounded Agent/Agents row to run-call rendering while preserving the upstream component.
- [x] 4.2 Cover named, parallel, duplicate, agentless, control-character, lifecycle-action, and no-renderer fallback cases.
- [x] 4.3 Confirm public schema, execution parameter protection, and lifecycle behavior remain unchanged.

## 5. Provenance and verification

- [x] 5.1 Update provenance/NOTICE for the bounded display and responsive-Matrix adaptations.
- [x] 5.2 Run focused tests, typecheck, full affected suite, provenance/release validators, package dry-run, and strict OpenSpec validation. Fresh authorized live subagent probes now bind the changed wrapper/test hashes and stable release validation passes.
- [ ] 5.3 After separately approved installation/restart, manually inspect dark-theme Matrix/reasoning, quota visibility, and named subagent header; do not perform a real provider run without exact approval.
