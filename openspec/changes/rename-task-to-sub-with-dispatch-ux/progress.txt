[2026-08-21T21:10:00+08:00] BUILD rename-task-to-sub-with-dispatch-ux EXECUTED
evidence=verification:BUILD-rename-sub-dispatch-ux
disposition=accepted
blocker=user acceptance feedback on the four-phase round: (1) YOLO auto-approve — resolveCurrentMode now returns the mode name alongside ModeDef, exported resolveCurrentPermissionModeName/isBypassPermissionMode reuse the exact session-entry/env/headless resolution chain; the preallocate model/thinking override confirm and the modelHub change confirm auto-approve under yolo (bypass doubles as the confirmation channel for headless hasUI=false sessions because confirmTaskModelRequest/ModelConfigurationService gate on hasUI before the callback), decision audited as the new overrideDecision value auto-approved-bypass, and sync settlements now attach modelDecision like async results; (2) task→sub rename across the full naming surface (registration guard/discovery/isCanonicalAiliTaskActive, runtime label/description/guidelines, native-integrations bypass, policy bridge + spawn-policy branch, child tool definitions/loadable/hard-denied, rose-context active check + sub.agent prompt paths, doctor evidence strings, live-release-support observers, docs/README/roles general.md) with no alias per the standing decision — old sessions fall back to the default renderer for historical task calls (display-only); (3) web parity — new lib/agent-dispatch.ts ports the TUI identity field selection, ToolCallBlock special-cases sub/task/formal_task/hub (collapsed identity row / action summary, expanded requested/effective/sources/overrideDecision/lifecycle/ids rows with raw JSON behind an explicit chat.showRaw disclosure), tool-execution-progress prefers TaskLiveSnapshot details over the JSON content line, and the new AiliAgentResultCard renders aili.agent-result deliveries with identity + provenance + collapsible preview. Evidence refreshed: roles.json general profileHash/sourceHash (verify:roles 21 roles), live-verification implementation map (20 files), adapter-evidence (10 artifacts; validate:compatibility 40 records). Tests: permission mode-name unit case; production yolo integration (two-model fixture asserts effectiveModel switch + auto-approved-bypass under print/no-UI); all rename suites updated (runtime/registration/collision/production/policy/package-runtime/formal-orchestration/doctor) 85/85; web colocated agent-dispatch 7/7, AiliAgentResultCard 2/2, MessageView +2 (13/13); typecheck clean; npm run build:web clean; openspec validate --strict valid; full npm test green except the pre-existing pi-0821 full-load flake
next_action=user restarts pi-web/pi and confirms: no override prompt under YOLO (decision visible in hub jobs), sub dispatch works with the new name, web shows subagent identity cards/progress/async result cards
