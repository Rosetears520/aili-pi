# Drift Log

> **Historical/superseded status (2026-08-01):** This log preserves dated drift and resolution history only. `openspec/changes/integrate-upstream-formal-agent-protocols` is the sole future BUILD and release owner for overlapping scope. No statement or session reference below independently authorizes dispatch, advancement, acceptance, closure, publication, or release; prior completion and verification claims were not independently reverified during this reconciliation.

## 2026-07-30 — BUILD_MATERIAL_DISCOVERY: bootstrap evidence compatibility

- The historical implementation record states that packages 2.1–2.3 implemented a strict persistent-runtime contract requiring raw Agent/job/turn identities plus `agent://` output and `history://` history references, ordered INSPECTED/DONE evidence, and a jointly valid pair before mutation. This was not independently reverified here.
- The dated self-validation record reports 23 diagnostics against the then-active bootstrap `tasks.md`/`progress.txt`. It also records that the board predated the parser, the external Task runner exposed only `agent://ses_*`, raw job/turn/history values were unavailable, and several progress blocks predated fixed event-field and terminal-anchor validation. The count was not independently reverified here.
- The recorded design conclusion was that fabricating unavailable IDs, silently weakening the strict persistent contract, or rewriting append-only history would violate the accepted evidence model.
- The open question at that time was whether v1 should add an honest bounded external-runtime/bootstrap reconciliation form or remain strict persistent-only. It is retained as history and cannot reopen superseded BUILD work.
- The log records that affected BUILD stopped before package 3.1 and that no dependency, lockfile, Git, publish, release, or unrelated source operation was performed; this operational claim was not independently reverified here.

### Resolution

- The dated decision record says the bounded bootstrap bridge was selected on 2026-07-30; this reconciliation does not treat a current-session reference as independent public authority.
- The historical DEFINE write-back added an exact identity/decision opt-in, real external session ref, explicit unavailable job/turn/history, append-only BOARD/package RECONCILED evidence, and `dispatch_timing=unverified-before-return`; strict persistent validation remained the stated default.
- The artifact records acceptance of the Round-2 regenerated final test plan with “接受并继续（推荐）” on 2026-07-30. That acceptance was not independently reverified here.
- Package 2.3a and the BOOT verification matrix were recorded as authorized at that time; this statement grants no current BUILD authority. Dependency/lockfile, Git, publish, and release operations remained unapproved.

## 2026-07-30 — BUILD_MATERIAL_DISCOVERY: production formal-root input

- The historical record says package 2.4 stopped before edits because production `registerPersistentAgentRuntime`, workspace leases, and `createWorkspaceMutationGuard` received no exact active `FormalTaskBoardRootPaths`; package 3.3's guidance provider carried phase/profiles/Owners only. This was not independently reverified here.
- The same record says the guard intercepted `write`/`edit`, while ordinary child bash required the permission sandbox for filesystem enforcement. Hidden prompt/session inference, broad OpenSpec scanning, and heuristic bash command parsing could not support the accepted immutable-owning-file claim.
- The recorded design conclusion was that completing production protection required a bounded task input plus fail-closed bash handling, changing the prior “no public task schema field” boundary.
- The dated decision record says “Add formal task context (Recommended)” was selected on 2026-07-30; this reconciliation does not treat the originating conversation as independent public authority.
- The historical DEFINE write-back added optional `formalContext: { changeId }`, exact same-root v1 validation, persistent protected paths, pre-write `write`/`edit` denial, exact sandbox `denyWrite` for formal bash, and formal-bash removal when that enforcement was unavailable. Ordinary task/default/permission behavior was stated to remain unchanged.
- Package 2.4 and affected BUILD were recorded as blocked pending Round-3 final-test-plan acceptance, with package 4.1 not started. This is historical state only.

### Resolution

- The artifact records acceptance of the Round-3 regenerated final test plan through “Accept and continue (Recommended)” on 2026-07-30. That acceptance was not independently reverified here.
- Packages 2.4 and 4.1 were recorded as eligible to return to ready under the accepted sync execution contract, with package 2.4 selected first. This no longer grants execution authority; dependency, Git, publish, and release operations remained unapproved.
