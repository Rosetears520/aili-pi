[2026-08-21T20:00:00+08:00] BUILD unify-context-pressure-timing EXECUTED
evidence=verification:BUILD-unify-context-pressure-timing-v1
disposition=accepted
blocker=user-requested v1: WHEN fully reuses the ACP pressure decision, HOW stays provider-routed. Implemented per design.md: vendored upstream/billion-context-pi gained src/pressure-evaluator.ts (createAcpPressureEvaluator — decision-only processTurn facade with renderTags "none", in-memory per-session CompressionState, shared configFor/resolveConfig incl. user acp.json merge on first observation, reset()) exported from src/index.ts; dist rebuilt in-tree (npm ci && npm run build) and the vendored node_modules removed afterwards because its second @earendil-works/pi-coding-agent instance breaks root typecheck and runtime resolution (dual-instance hazard documented in context.md). AILI side: new src/runtime/context-pressure.ts wires turn_end observation (official trigger-compact.ts pattern) gated on the codex owner with an in-flight guard and ctx.compact({onComplete/onError}), a session_before_compact threshold gate ({cancel:true} only for reason=threshold on codex turns; manual/overflow pass), session_compact baseline reset, and switch/shutdown hygiene; context-runtime.ts composes acp -> pressure -> codex so the gate short-circuits before the codex-compact handler, with an injectable pressureEvaluator option. Non-Codex path untouched. Tests: new tests/unit/context-pressure.test.ts (8 wiring cases + 1 real acp-kernel decision case proving baseline -> 30K growth + ~51K compressible triggers shouldRelieve below the emergency line -> reset returns to baseline), codex-remote-compaction-compat.test.ts now asserts 3 session_before_compact handlers with the gate matrix and separate acp-owned instance for the non-codex cancellation. Governance: inventory test pins createAcpPressureEvaluator in src+dist plus the decision-only/no-store markers; provenance.json symbols/localChanges/verification and THIRD_PARTY_NOTICES.md updated. Verified: typecheck clean, 5 focused suites 18/18, npm run validate:capabilities registry validation passed, openspec validate --strict valid, npm run validate:compatibility 40 records, full npm test 689 passed with only the pre-existing pi-0821-model-metadata full-load flake (reproduced on pristine tree in stage 1)
next_action=proceed to make-persistent-agent-model-thinking-decisions-explicit (stage 3)
