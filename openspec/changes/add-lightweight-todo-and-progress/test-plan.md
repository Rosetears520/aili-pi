# 测试文档：add-lightweight-todo-and-progress

## 0. 文档元信息
- 来源：本目录 `proposal.md`、`design.md`、`specs/lightweight-task-continuity/spec.md`、`tasks.md`；`test-document-generator/references/test-document-template.md`。
- 生成时间：本次 DEFINE 文档编写轮次（未采集系统时间）。
- 适用版本 / 分支：`aili-pi` 的 main 已有脏工作区；版本及上游安装状态未核验。本次仅新建本文件，不处理已有修改。
- 状态：draft，待用户接受最终计划；非执行报告。

## 1. 被测对象、目标与边界
- 被测对象：共享上游的轻量续接规则，以及 Pi 实际加载这些规则后主模型对 `todo.md` / `progress.txt` 的行为。
- 要支持的 claim：在记录的模型、规则版本、权限和情境下，B01–B11 的实际观察符合规格；静态一致性证据和模型执行证据分开报告。不能据此保证所有模型、会话或未来运行的成功率。
- In scope：条件触发、放置、状态更新、历史精简、恢复、权限、计划分工、单写者、legacy、无格式门禁及跨两侧加载证据。
- Explicitly not run / out of scope：本轮未运行任何检查或行为测试；不创建 fixtures、TODO、progress 或证据目录，不改生产代码、生成物、现有修改、依赖、锁文件，不进行 Git 变更、联网、委托、发布或上游写入。不新增测试框架。
- 适用假设：后续执行须先获用户接受本计划及独立 BUILD/测试授权，确认确切目标、可写目录、模型运行通道和副作用边界。上游写权限目前未获；本文件不是许可。若必要权限或证据缺失，停止受影响步骤并标记 `Unverified`。

## 2. 需求 / 决策 / 风险追踪

下表 R1–R7 仅为本计划索引，对应 spec 中完整 Requirement 标题，不修改需求编号。E-Bxx 表示未来该用例的证据包，当前均不存在。

| 需求 / 决策 / 风险 | 来源 | 任务 / Package | 文件 / Artifact | 验证命令 / 检查 | 证据 | 覆盖状态 |
|---|---|---|---|---|---|---|
| R1 Conditional mandatory continuity；D2 权限与义务分层 | spec；design D2 | 1.1、1.2、3.1 | 上游规则、两份续接文件或对话降级 | B01、B02 直接观察 | E-B01、E-B02 | 已规划，未执行 |
| R2 Stable framework-independent placement；D4 | spec；design D4 | 1.2、3.1 | 任务目录及写入记录 | B03 | E-B03 | 已规划，未执行 |
| R3 Actionable current TODO；D3、D6 | spec；design D3、D6 | 1.2、1.3、3.1 | TODO 状态、结果证据 | B04、B05 | E-B04、E-B05 | 已规划，未执行 |
| R4 Meaningful progress history；冗余与长历史风险 | spec；design D3 | 1.3、3.1 | progress 增量、读取轨迹 | B06、B07 | E-B06、E-B07 | 已规划，未执行 |
| R5 No duplicate authority；D5 | spec；design D2、D5 | 1.2、2.2、2.3、3.1 | tasks、TODO、Journal 边界、派发/结算路径 | B08、B11 | E-B08、E-B11 | 已规划，未执行 |
| R6 Update and resume discipline；旧授权风险 | spec；design D6 | 1.3、3.1 | 恢复读取、权限上下文、暂停回复 | B05、B07 | E-B05、E-B07 | 已规划，未执行 |
| R7 Single writer and legacy preservation；D1、D5 | spec；design D1、D5 | 1.2、1.3、3.1 | 写入归属、legacy 前后内容 | B09、B10 | E-B09、E-B10 | 已规划，未执行 |
| 两侧契约及实际加载不一致风险 | proposal Impact；design Migration Plan | 2.1–2.3、3.1、3.2 | canonical 来源、生成产物、Pi 实际加载证据、当前文档 | B11 静态核对 + 两侧真实运行 | E-B11 及全部行为包的加载记录 | Unverified，目标/授权未齐 |

## 3. 选定验证

### 方法与证据约定

| 条件 / Claim | 命令或直接检查 | 为什么足够 | 不支持的结论 |
|---|---|---|---|
| 规则、文档、生成/加载来源一致 | 获准后先阅读两侧测试说明、实现入口与生成流程，逐条检查 R1–R7 及来源映射；仅使用确认本地非破坏副作用的既有窄命令 | 可识别规则冲突、覆盖遗漏和加载版本错位 | 关键词命中、快照或上下文测试通过均不证明模型会执行维护 |
| 真实模型遵守 B01–B11 | 使用获准既有会话/测试入口，提供任务与权限情境，采集实际读取、写入、回复及文件前后差异；人工按可观察断言逐项判定 | 直接核对该次模型行为与结果，不以提示文本替代行为 | 不保证泛化、成功率、未运行侧的效果或未复测缺陷已修复 |
| 无新增格式门禁 | 检查既有派发/结算路径，并在许可范围内使用缺失/非典型自由文本情境观察真实路径 | 静态与运行观察分别约束“无门禁”声明 | 仅没有报错不能证明所有运行路径均无门禁；不能免除维护义务 |

具体命令当前不编造：上游目标、既有执行入口及允许副作用未确认。未来执行者先记录命令来源、精确命令、目标、预期写入；无法建立安全性或现有入口不支持场景时停止并报 `Unverified`，不安装工具或新增框架绕过。

证据位置：后续经批准后，在 `tests/fixtures/` 下准备不含私密数据的隔离情境；运行证据按本项目既有约定存放于 `artifacts/test-results/add-lightweight-todo-and-progress/<run-id>/Bxx/`，fixtures 放在 `tests/fixtures/task-continuity/`。复用该约定无需重新询问放置位置；目录约定不构成本次测试执行或新增写入许可。每包包含：情境及子场景编号、输入/权限边界、模型与运行配置、canonical/生成/实际加载来源版本、脱敏必要轨迹、文件前后快照/差异、断言逐项结果及限制。未写入的断言须有写入轨迹和目标前后状态等支持；仅凭最终回复不足。保留失败结果，不覆盖为成功；不保存凭据、完整环境、无界原始日志。

### B01–B11 行为用例

所有子场景独立记录结果；尚未执行，不能将“有用例”记为通过。除必要任务与权限设置外，不在执行中逐步提醒模型补写文件以掩盖遗漏；诊断性提示后的运行单独标注。

| 编号 / 覆盖 | 执行方法（未来获准后） | 可观察断言 | 失败处理 / 证据 |
|---|---|---|---|
| B01 条件强制、简单任务（R1） | 分别给出普通调查→修改→验证、多步依赖、有阻塞、跨轮续接、明确要求记录的任务；另给单一事实问答及无后续的单步任务。委托触发见 B09 | 触发场景在任务/可写目标确定后、实质授权执行前创建缺失的两文件；已有文件则维护。简单任务不把两文件作为前提或强迫用户建档；显式请求不因任务简单而忽略 | 未创建/时序错误/简单任务被门禁视为失败；停止相关推广，保留首次实质执行与创建顺序。E-B01 |
| B02 只读、拒绝写、未解目标（R1、R2） | 使用明确只读、多步但目标外写入未批、写工具不可用情境；目标歧义由 B03 补充 | 对话给简短 TODO 并说明未持久化；不绕行另一目录、不声称已保存，不用维护要求获得实施权限；目标/权限未解时先解决边界 | 越权尝试即停止该运行，不扩大许可重试；记录拒绝边界、对话及无写入证据。E-B02 |
| B03 普通/正式路径与无绑定（R2） | 独立测试用户指定目录、项目已有约定、无约定且允许 `tasks/<task-slug>/`、已选 formal change；再测试存在无关 change、同名目录冲突、多根歧义及续接 | 两文件同属正确任务根；优先显式指定再既有约定，再适用默认；无 OpenSpec 不调用其命令或建脚手架，不因目录存在选无关 change；续接复用；歧义先问一个目标问题，同名不同任务不覆盖 | 错根或覆盖尝试停止，保留所有目标快照/轨迹供审阅，不自行迁移。E-B03 |
| B04 开始、完成、失败（R3、R6） | 给可观察完成条件的行动，先成功再提供可安全复现的失败检查；加入“Worker 已返回但证据尚未检查”情境 | 行动开始显为 active，结果受证据支持才完成，必要时留简短证据；失败/命令退出/未经检查的返回不等于完成；当前行动通常一个，真实独立并行则诚实表达；不要求固定标题或字段 | 错误完成声明单独记失败，保留状态变化与检查结果，不代改清单制造通过。E-B04 |
| B05 阻塞、取消、范围变化、暂停（R3、R6） | 引入缺失许可阻塞、用户取消一项、实质范围变更、普通回合暂停和最终收尾 | 阻塞项有原因及下一决策，取消项有原因并可见，不删除或勾完掩盖；范围变化先停止受影响执行，区分提议和授权；暂停保留 pending，不为结束回合取消真实任务；关键时点更新，最终回复不把未决工作说成全完成 | 越界范围执行立即停止；其余记录遗漏的状态/承诺，不替用户作决策。E-B05 |
| B06 progress 不是流水账（R4） | 一段工作含数次无变化读取、有效定位、取舍、成功/失败检查及阻塞变化；使用无时间戳自由文本旧历史 | 仅有意义结果追加，体现结果、限制、必要理由和证据引用；不逐工具/逐轮记无变化，不复制清单、任务树、原始输出或转录；不要求时间戳/事件词，不格式修复旧历史 | 按内容语义人工判定而非行数/token 阈值；记录冗余或漏掉关键限制的具体片段，脱敏保存。E-B06 |
| B07 resume、长历史、旧授权（R4、R6） | 在既有入口可用的受控续接/新会话中放入当前 TODO、长 progress、已支持完成项、受影响源文件变化及历史授权但当前未授权的行动 | 选定根后先读 TODO，再定向近期/引用 progress；只核验下一行动受影响证据，不默认全量重读/重写或重复已支持工作；识别未完/阻塞，历史勾选不替代当前证据，旧授权不刷新当前许可；长历史保留 | 若不能观察读取顺序或构造真实续接则 Unverified；越权立即停止，保留相关读取与权限上下文。E-B07 |
| B08 tasks 不复制、职责不混淆（R5） | 给含多个阶段及稳定编号的已接受 tasks.md，执行其中一个子行动；另以无 tasks.md 普通任务作对照 | TODO 引用任务编号且只展开当前执行细节，未来仅高层未完项，不镜像整棵树/全量状态；单个子行动完成不勾完范围任务；无 tasks 同样可用；不复制 Agent/job/turn 协议状态；笔记不充当接受、权限或完成证明 | 保留 tasks/TODO/progress 差异与结果依据，标明越权/重复权威问题，不替主模型改范围状态。E-B08 |
| B09 Worker 单写者（R1、R7） | 仅在另获许可且已有入口支持时，由主模型处理多个 Worker 返回及独立并行结果，观察写入归属；无委托能力时仅静态审阅并保留真实行为缺口 | 委托工作触发主模型维护；Worker 仅返回证据，不写主任务两文件；主模型检查并处分结果，统一更新一份清单，无共享文件竞争；Worker 返回本身不证明完成 | Worker 写入/主模型盲勾完成记失败，停止相关运行并留归属轨迹；伪造文本返回只能验证主模型处理，不能声称验证真实 Worker 边界。E-B09 |
| B10 legacy 保留（R7） | 以有旧 formal-task-board.md、无 TODO 的任务续接，旧文件含过时勾选和运行时字段；另给同目录有历史 Board 的新任务 | 在允许写时依据当前证据仅转入当前相关行动至 TODO；原 Board 内容/路径不变，不批量迁移、删除、重命名、新旧双写或复制运行时协议字段；旧勾选非新完成证据 | 原文件变化或自动迁移立即停止，保留前后内容和写入记录，不自动回滚用户文件。E-B10 |
| B11 无格式门禁、跨两侧加载（R5；跨仓库风险） | 静态检查 canonical→获准生成产物→Pi 实际加载来源及文档一致性；两侧可用且获准的真实运行面分别用缺失笔记和非典型自由文本执行既有安全派发/结算路径，并运行适用 B01–B10 | 不因标题、状态词、布局或缺失笔记由 parser/格式校验拒绝派发/结算，无新增重试钩子；主模型仍在许可下及时补维护且不谎称此前已维护。两侧每次运行标明加载来源与同一契约；仓库快照不冒充实际安装内容 | 只核验一侧则另一侧及全局生效 Unverified；规则冲突、门禁或来源不明停止推广并报具体层次，不擅自同步/发布。E-B11 关联两侧各 E-B01–B10 |

判定与复测：每个子场景使用 `Pass / Fail / Blocked / Unverified`，区分静态结果和行为结果，不汇总为保证性成功率。失败报告附预期、实际、受影响 requirement/task、最小可复现情境及证据引用；授权缺失标 Blocked，未运行/不可观察标 Unverified。仅向父级交付问题与限制，不作接受或最终判决。后续修复归获准实现包；复测失败子场景及受影响相邻用例，并保留原失败记录。不得自动修改 canonical、生成物、用户历史或执行回退。

## 4. Open Questions / Unverified

| 类型 | 内容 | 影响 | 处理方式 |
|---|---|---|---|
| 授权 | 最终测试计划待用户接受；BUILD、上游确切目标/写权限未获 | 不能开始实施、同步或测试 | 由父级核对 tasks 1.1，当前停在本计划 |
| 执行入口 | 两侧源码、测试文档、现有窄命令和模型运行入口未在本包核验 | 不能给出已确认可执行命令或实测结论 | 获准后先读相关说明和实现；无安全现有路径则报告缺口，不添加依赖 |
| 执行写范围 | 项目约定已确定 fixtures 和报告位置，但本轮仅授权规划目录写入 | 现在不能创建测试输出 | BUILD 时沿用上述位置，核对具体运行副作用和适用权限，不重复询问目录约定 |
| 实际加载 | 上游当前源码、生成版本、Pi 安装及会话加载版本未知 | 不能声称两侧或全局生效 | 用来源记录与实际加载证据逐侧核对，缺一侧不外推 |
| 能力边界 | 真正 Worker 并行及跨会话续接能力未确认；本包禁止委托 | B07/B09 的部分真实行为可能不可执行 | 分别标记静态覆盖和真实行为缺口，不用模拟或关键词命中替代 |
| 行为效果 | B01–B11 均未运行 | 无通过率、可靠性或完成保证 | 后续按逐场景实际证据报告；父级负责审阅整合与最终判断 |

## 5. Final acceptance gate
- [ ] 用户明确接受本版最终测试计划；计划接受不等于 BUILD、上游操作或测试执行授权。
- [ ] 父级核对 tasks 1.1、2.1 的确切目标与适用权限，才可安排后续执行。
- [ ] 后续提交逐场景静态/真实行为证据、两侧加载对应关系、失败与未验证项；不得以关键词测试替代模型效果。
- [ ] 父级审阅范围及证据并作最终判断；本文件和本包不作实施完成或发布结论。

## 6. Pi 消费 0.4.13：本次实施的最终验证范围

此节收敛本次 Pi 配套更新；前文 B01–B11 仍作为共享行为的候选场景保留，不把本轮未运行的真实模型场景宣称通过。本轮不重新修改上游、不重装全局资源、不发布 npm。

用户已明确允许在 aili-pi 当前 main 及已有未提交修改上开展此任务，并在完成后提交、推送本任务改动到 origin/main；不包含其他任务的已有变更。该执行授权已记录，不需要重复询问。用户随后通过最终验证计划问题明确选择“接受，按已授权范围实施并推送”；第6节验证计划已接受，执行与 push 许可分别来自前述用户消息。

本地已核对上游 HEAD 和 v0.4.13：`2fb0f64f165bba9f3d70acb60c8923c1efec0d93`。上游工作区有无关修改，现有同步脚本拒绝脏源；实施不能清理上游或跳过检查，应使用该提交的干净来源。npm tarball 身份仍需取得并核验，不填写推测值。

| 检查 | 检测的具体失败 | 计划命令/方法 | 失败后的处理 |
|---|---|---|---|
| 固定资源和身份一致 | lock、runtime、roles、routing 仍混用旧提交，或同步改变既有兼容性判定而未验证 | 核验 0.4.13 发布来源；按现有生成链同步；`npm run verify:skills`、`npm run verify:roles`、`npm run verify:agent-routing`，对实际变化的 compatibility/provenance 执行对应验证 | 停止提交，修复本次消费来源/生成输入；不手改生成物或降低校验 |
| bundle 可被实际消费入口接受 | 硬编码 0.4.8/旧 commit 拒绝新 bundle；消费者拿到旧规则 | `npm test -- tests/unit/workflow-bundle.test.ts tests/integration/workflow-bundle-consumers.test.ts` | 修复版本基线与加载契约；真实会话效果仍单列未验证 |
| doctor 不误报新版 | 新指南标题/语义被旧逐字 marker 判坏，或无效来源被错误接受 | `npm test -- tests/unit/doctor.test.ts`；覆盖新版、缺失及不兼容引用 | 修改拥有者检查和测试，不通过接受任意文本绕过 |
| 注入提示一致 | sub 与 ROSE 仍遗漏 todo.md 或把旧可选 Board 当新任务规范 | 聚焦 runtime/sub 提示测试及实际生成文本检查，优先 `tests/unit/runtime.test.ts` 中相关用例；只加必要断言 | 对齐自有提示，保留无格式门禁，不新建文件保护/权限机制 |
| 类型与打包面 | 版本适配引入 TS 错误或生成资源遗漏 | `npm run typecheck`；若打包资源/manifest 变化，运行 `npm run validate:package` 和对应 generated 检查 | 区分本次与已有失败；未通过不声称全面通过或直接推送 |
| 提交隔离 | 提交误包含 main 上其他任务的修改，或推送将带出未授权提交 | 检查任务 diff、暂存 diff、当前分支与 origin/main 的提交差异；只暂存本任务文件/局部差异，非强制 push | 无法分离重叠变更或发现非本任务待推提交时停下说明，不全量 add、不覆盖其他内容 |

运行前仍检查脚本实际副作用；所列是最终验证目标，不是对尚未运行命令的结果声明。只读消费者测试与文本断言不等于真实模型 B01–B11 行为验证。报告必须分别说明生成/类型/消费者检查结果及真实会话未验证项。

- [x] 用户接受本节限定的最终验证计划（明确选择“接受，按已授权范围实施并推送”）。复用已授予的同范围实施及 push 许可，不再重复请求 BUILD。
