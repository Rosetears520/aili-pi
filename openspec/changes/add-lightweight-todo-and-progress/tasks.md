本次已接受的 Pi 消费范围以 `test-plan.md` 第6节为准。第1组为先前上游任务（由其他会话交付，本轮未重新执行）；3.1 为尚未执行的共享真实模型验证，不纳入本次 Pi 配套完成声明。用户后续已授权本次 main 提交/push，不包含 npm 发布或其他任务改动。

## 1. 实施前置与上游规则

- [ ] 1.1 ROSE 核对本版 test-plan.md 的用户接受、BUILD 授权和上游确切目标/写权限；验证对应用户事件及目标规则，不以本提案代替许可。
- [ ] 1.2 上游 implementer 在获准目标定位治理源及 delivery-flow 续接引用，加入条件强制 todo.md、路径规则、单写者与权限例外；验证新规则不再把新 TODO 定义为可选，且不新增运行时格式门禁。
- [ ] 1.3 同一上游包更新 progress 分工、关键更新时机、恢复顺序与旧 Board 处理说明，加入自有轻量示例；按 test-plan B01–B11 检查规则及可执行行为样例的覆盖。

## 2. Pi 消费与文档一致性

- [x] 2.1 ROSE 确认上游产物与 Pi 侧识别同一契约，并获得精确同步/锁文件权限；验证来源记录和目标边界，不自行发布或升级依赖。
- [x] 2.2 Pi implementer 使用已有生成流程同步获准来源并对齐受影响运行时提示；验证生成一致性及聚焦上下文测试，禁止直接编辑 skills 快照。
- [x] 2.3 更新 docs/persistent-agents.md 及必要的当前说明，清楚区分 todo.md、progress.txt、tasks.md 与 Journal；验证文档和实际加载提示一致且不声称已安装内容自动更新。

## 3. 聚焦行为验证与结果交付

- [ ] 3.1 test-engineer 在 tests/fixtures/ 下准备获准的轻量情境及可用既有执行路径，不添加依赖或新框架；执行 test-plan B01–B11 并记录模型/加载规则和实际文件行为，不以文本命中代替模型行为。
- [x] 3.2 ROSE 检查任务范围 diff、来源/消费者证据及未验证项，完成当前 Pi 消费候选隔离验证：9文件85项测试及类型检查通过。检查仅覆盖本任务，保留原有未提交内容；按后续用户明确授权进行任务隔离提交/push，不发布 npm。证据见 pi-0.4.13-verification.md。
