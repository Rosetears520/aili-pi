# Tasks: publish-aili-pi-preview

**Target:** `@rosetears/aili-pi@0.2.0-preview.0` under the npm `preview` dist-tag, after exact separate publication approval.

**State:** DEFINE complete — the final preview test plan is accepted. Affected BUILD still requires fresh explicit implementation authorization; installation, version/lockfile mutation, Git, and publication remain separately unauthorized.

## 1. Contract definition

- [x] 1.1 Record the user decision: local trial before non-`latest` preview publication; stable release remains blocked.
- [x] 1.2 Record the proposed preview identity: `0.2.0-preview.0` and `preview` dist-tag.
- [x] 1.3 Strict-validate this change and obtain explicit acceptance of its final `test-plan.md`.

## 2. Preview candidate proof

- [x] 2.1 Under separate version/lockfile approval, set the exact prerelease identity through all owned metadata and regenerate owned provenance/package evidence.
- [ ] 2.2 Implement and pass the quality-enabled official-Pi one-block vertical slice without changing production quality defaults or permissions.
- [ ] 2.3 Under separate disposable-install approval, run the local package/CLI discovery smoke and record only sanitized candidate-bound evidence.
- [ ] 2.4 Update preview documentation to use the exact prerelease/local-tarball route, preserve the quality limitation, and avoid `latest` or stable-ready claims.
- [ ] 2.5 Produce an exact locally packed preview candidate and a redacted user-trial briefing.

## 3. User trial and preview publication

- [ ] 3.1 Obtain the user's explicit exact-target approval for local installation and any configured-Provider/cost/privacy exposure.
- [ ] 3.2 Record the user's redacted manual-trial result against the exact candidate identity.
- [ ] 3.3 If accepted, request one separate exact npm publication approval for `@rosetears/aili-pi@0.2.0-preview.0` with `--tag preview`; otherwise stop without publication.

## 4. Stable-track boundary

- [ ] 4.1 Keep the stable `0.2.0` release-gate contract non-PASS. Do not mark its evidence, human-review, validation, or publication rows complete from preview work.
