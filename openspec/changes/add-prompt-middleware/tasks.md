## 1. Definitions and Resolution

- [x] 1.1 Implement modifier schema, trusted discovery, hashing and duplicate/traversal diagnostics with unit tests
- [x] 1.2 Implement scope/role allowlist/requires/conflicts and deterministic ordering with unit tests

## 2. Assembly and Runtime Policy

- [x] 2.1 Implement stable-prefix/dynamic-block assembly and verify deterministic prepend/user/append output and stable prefix hash
- [x] 2.2 Implement monotonic runtime policy patches using existing permission/tool intersections and verify forceReadOnly blocks write/edit/mutating-or-unknown bash

## 3. TUI and Subagent Integration

- [x] 3.1 Implement session-scoped pending state, Alt+S, `/snippets`, preview and consume-on-success one-shot behavior with TUI tests
- [x] 3.2 Add role snippet allowlists and optional `sub.snippets`, validating before child startup and proving continuation does not persist one-shot modifiers

## 4. Provenance and Closeout

- [x] 4.1 Implement bounded applied/rejected provenance projections with credential/private-body redaction tests
- [x] 4.2 Update manifests/docs/clean-room notices and run focused typecheck, middleware tests and strict OpenSpec validation
