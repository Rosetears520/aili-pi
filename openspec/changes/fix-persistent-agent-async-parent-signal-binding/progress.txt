[2026-08-21T19:50:00+08:00] BUILD fix-persistent-agent-async-parent-signal-binding EXECUTED
evidence=verification:BUILD-parent-signal-binding-fix
disposition=accepted
blocker=implemented per design.md D1: TaskCoordinator.submit() now derives parentBoundTasks (!effectiveAsync || ancestry !== undefined) and scopes listener registration, the already-aborted immediate trigger, and the allSettled cleanup to that subset; top-level accepted async tasks no longer observe the submitting turn's signal at all. runLifecycle()'s post-executor aborted check and every explicit cancellation channel are untouched. Supporting fix discovered by task 3.4: FifoTurnScheduler.runNested() never registered nested entries anywhere cancel() could reach (nestedTails only chains promises), so the parent-signal listener's cancel(jobId) returned not-found and nested tasks could not actually be aborted by ancestor cancellation; scheduler now tracks nestedRunning controllers, cancel() aborts them (returns "running"), and close() aborts them alongside queued/running entries. Tests: six new regressions in tests/unit/persistent-agent-task.test.ts (async survives abort + delivery, sync cancelled with aborted lifecycle, mixed batch cancels only the sync item, nested follows parent task signal, completion-boundary abort keeps generated output, pre-aborted signal runs async / cancels sync) with an abortRace helper because AbortSignal never replays past aborts to late listeners; 25/25 green. Evidence ledger: manifests/adapter-evidence.json subagent.dispatch artifact hash refreshed for the modified test file (npm run validate:compatibility verifies 40 records). npm run typecheck clean; openspec validate --strict passes; full npm test 679 passed with one pre-existing full-suite timeout flake in tests/integration/pi-0821-model-metadata.test.ts reproduced identically on the stashed pristine tree (passes standalone)
next_action=none; ready to merge alongside the unify-context-pressure-timing change
