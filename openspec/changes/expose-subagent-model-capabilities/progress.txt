[2026-08-29] DEFINE proposal created from user-reported subagent preflight failures. Repository and Pi 0.84.4 evidence confirms explicit target model with omitted thinking can inherit Parent xhigh before managed/Herdr startup. User decision: omitted thinking uses the target model default; it does not inherit Parent thinking and does not automatically select the highest supported level.
[2026-08-29] Planning artifacts completed: proposal, three capability specs, design, tasks and draft test plan. Design uses the current Pi available/authenticated/scoped catalog, trusted `input` provenance (`interactive|rpc`) for current-turn authority, bounded `before_agent_start` system-prompt injection, current isolated-child Pi default (empty in-memory settings → medium → target clamp), and one shared managed/Herdr preflight. Independent plan audit gaps were written back: Pi prompt chaining uses returned `systemPrompt`; trusted `input` provenance is correlated once by normalized digest; model scope uses one shared predicate; persistent child default is the existing empty in-memory settings path (`medium` → target clamp); modality/tool claims and identifier bounds are explicit. User accepted the Pi provenance boundary: earlier installed Extension transforms remain user-originated when Pi preserves `interactive|rpc`; later prompt mismatch fails closed. Material delta accepted for DEFINE merge: `sub` remains the sole delegation entry; omitted CLI means Pi; an explicitly current-turn named external CLI runs as a one-shot nested command inside a visible Herdr Pi Agent after command-v/version/help discovery; native external Drivers are deferred. The AILI-owned billion-context factory permanently disables `acp_delegate*` while retaining compression/context, and user/project ACP config cannot override the factory disable. Plan audit then required atomic CLI authority/backend preallocation, an immutable built-in modifier, a deterministic bounded version/help probe helper, source/dist ACP verification, and honest external-process boundaries. User accepted normal HOME for explicit CLI tasks: the vendor CLI may use its existing auth/config/cache and inherent provider network/source behavior, while AILI does not inspect or persist credentials. Proposal, design, external-CLI capability spec, tasks and draft test plan were expanded accordingly. Cross-change write-back completed before implementation: `make-persistent-agent-model-thinking-decisions-explicit` retains independent hub-send work while its conflicting model-only Parent-thinking fallback is superseded; `restore-native-working-ui-and-expose-agent-model-resolution` delegates model catalog/preflight/transparency to this change while retaining Working-animation scope.
[2026-08-29] On 2026-08-29 the user accepted the revised test plan and authorized repository-local BUILD, explicitly authorizing `openai-codex/gpt-5.6-terra` with `xhigh` for `aili.implementer`. Real vendor CLI/provider, real Herdr pane, Browser/E2E, dependency/lockfile, user-home mutation by AILI, Git, publish and release remain unauthorized. Overlapping-change supersession/dependency write-back is the first BUILD task.
[2026-08-29] BUILD executed. aili.implementer (gpt-5.6-terra, xhigh) ran ~29 minutes and ended with a provider stopReason error after completing the core implementation (model catalog projection, current-turn authority capture, model-default thinking fix, atomic CLI preallocation, immutable external-cli runner modifier + probe helper, billion-context delegation retirement, docs). Parent (ROSE) inspected every changed file on disk, kept the implementation, and completed the tail: corrected the model-selection GLM regression fixture (stale oneShot.thinking), rewrote the external-cli test matrix with full PATH isolation (version→help, missing, non-zero exits, no-safe-mode, generic grok identity ambiguity, 64KiB truncation with credential redaction, hanging probe timeout, pre-abort, authority validation), guarded before_agent_start against synthetic contexts without modelRegistry, refreshed adapter-evidence hashes + sync:adapters, regenerated provenance notices with the dist-disposition record, excluded the stale billion-context dist from the npm package, exported herdrRunLoadout for the runner-modifier/loadout-hash test, and added the four remaining coverage gaps: SUB_CLI_DENIED + SUB_CLI_MANAGED_CONTINUATION preallocation failures, applyUserConfig monotonic delegate retirement, and the isolated-child settings-leak negative fixture (parent xhigh settings cannot raise the medium→clamp default). Verification: focused files 66 tests; full suite 128 files/891 tests — 888 passed, 2 skipped, 1 pre-existing web-foreground readiness flake under full-suite load (passes in isolation; unrelated to this change); typecheck, validate:package, validate:generated (21 roles/20 routing), validate:provenance, validate:capabilities, validate:compatibility (40 records), doctor, and audit-redaction all pass. Not executed (unauthorized per accepted plan): real vendor CLI/provider execution, real Herdr pane, Browser/E2E, dependency/lockfile changes, Git operations, publish/release.
