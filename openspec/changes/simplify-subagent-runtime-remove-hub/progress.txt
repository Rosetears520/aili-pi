[2026-08-21] BUILD 实现（tasks 1–6.4 全部完成）
- 新 sub 协议落地：SUB_TOOL_SCHEMA/validateSubRequest（sub-schema.ts）；公开 submit 前台默认 + background 显式；嵌套 background 拒绝。
- task_id 续接：SubCoordinator.continuableAgent/continueAndSchedule；executor continuation:true 走 sendUserMessage；SUB_BUSY/SELECTOR_MISMATCH/TERMINAL/NOT_FOUND/OWNERSHIP/FORMAL_CONTINUATION_REFUSED。
- Hub 全删（hub.ts、注册、renderers、modelHubOperation、revive、mailbox）；/sub-cancel 用户命令；park 仅内部回收。
- 严格模型语义：层序重排（本 Turn 授权最高）；resolveSubModelIdentifier compact 别名；显式失败码 SUB_MODEL_UNAVAILABLE/AMBIGUOUS/DENIED/THINKING_UNSUPPORTED；删除静默 fallback。
- 空结果双层修复（assistantText 跳过 tool-only + SUB_EMPTY_RESULT invariant）；history 逐条 redact + redactedSensitiveEntries。
- 命名清理 task-*→sub-*（git mv）；runtime.task→runtime.sub；外围（doctor/native-integrations/formal-orchestration/live-evidence-contract/live-release-support/Web）同步。
- 测试迁移与新增；全量 npm test 688 passed / 2 skipped（live-gated）；typecheck、validate:package、validate:generated、adapter-evidence --verify 通过；Web node --test 通过。
- manifests/adapter-evidence.json 刷新（重命名路径映射、删除 hub 测试工件、新哈希）；live-verification.json 保持历史 NON_PASS 不动。
- README 与 docs/persistent-agents.md 重写为 sub 单入口模型。
[2026-08-21] 待办：6.5 真实 provider 验收（需真实 GLM/Luna 环境）后回填 test-plan 证据。
[2026-08-21 22:30] 真实会话冒烟反馈修正：用户实测并发场景时模型把"并发"等同为 background=true（Turn 立即结束、结果后置投递），与期望的"像工具调用一样好了再结束 Turn"不符。核实官方 Pi agent-loop：同消息多工具默认并行执行（executeToolCallsParallel + Promise.all，toolExecution 默认 "parallel"）——即"同一条 assistant 消息发多个前台 sub"已天然获得并行+阻塞语义，无需机制改动。修正引导文案（runtime.ts 描述/片段/指南、sub-schema background 字段描述、docs、README）：并发=同消息多个前台调用；background 仅限跨 Turn fire-and-forget。相关断言与 adapter-evidence 哈希已同步；typecheck 0 错误，npm test 688 passed / 2 skipped。另：冒烟中 glm5.1→zai-coding-cn/glm-5.1、gpt5.6luna→openai-codex/gpt-5.6-luna 紧凑别名解析按设计生效。
[2026-08-21 22:40] 按用户决定隐藏公开 background：SUB_TOOL_SCHEMA 移除 background 字段；validateSubRequest 对显式携带 background 的调用返回自纠错错误（"disabled in this build: omit it and run foreground — parallel work is several sub calls in the same assistant message"）；工具描述/指南改为纯前台心智模型（并发=同消息多个 sub，Pi 默认并行执行并等待全部）。AsyncDeliveryService/submitTrusted 内部异步机制完整保留（formal 生命周期仍用），未来可无迁移恢复该字段。相关单测/集成断言迁移（异步机制覆盖改走 submitTrusted；SUB_BUSY 用前台 gate 构造运行态）；typecheck 0 错误，npm test 688 passed / 2 skipped，adapter-evidence 已刷新验证。另核实角色目录：loadRoleProfiles 仅加载 hash-locked 的 manifests/roles.json + roles/*.md（恰好 20 个 selector），全部可经 subagent_type 调用；无运行时自定义角色机制，新增角色需走仓库生成管线。
[2026-08-22] 按用户决定移除 formal_task 工具：模型面只剩 sub 一个委派工具。formal-task-board.md 改为通用任务看板/todo 约定（项目根、无 openspec 依赖、运行时不校验）：sub 工具描述与指南明确"看板即 todo——包状态 pending/ready/running/done 由编排方自己维护，Owner 作 subagent_type、包 id 进 description，经 sub 派发并自行回写结果"。删除 formal-task-tool.ts 与嵌套 formal_task 注册；doctor/web 工具集/进度旁路同步；board 校验等 formal 内部机制保留为惰性内部路径（submitTrusted 仍可达，供既有测试与未来复用）。全量 687 passed / 2 skipped；typecheck 0；adapter-evidence 刷新验证；web 重构建。
[2026-08-22] 确认并钉死“sub 零文件依赖”契约：公开 submit 的 item 永不含 formalContext（schema 拒绝），resolveFormalProtection 首行守卫即返回，board 校验对 sub 不可达；新增回归测试“public sub depends on no project files”（空项目目录跑通完整 turn 且项目目录零文件产生，断言 readdir 为空）。
[2026-08-22] 按用户决定完成 aili-pi 本地解耦：多步骤工作仍创建自由格式 progress.txt，但不做格式校验；formal-task-board.md 改为可选任务备注。trusted formal protection 仅校验安全 change id 并派生两个受保护路径，不读取 Board/progress 内容。新增 OpenSpec capability/design 与 aili-workflows 跟进文档。聚焦 Vitest 16 passed，typecheck 通过，OpenSpec 1.10 定向 strict validate 15ms 通过。
[2026-08-24] 已消费 canonical rose-aili 0.4.8：GitHub release run 32687088484 成功；commit/npm gitHead=a5284ee105a084392a944aee04313dcf7c294a64；npm tarball SHA-256=cfc3b90e982900dcc17cf841066f6eee022cc2b97f27bb40e39494ac77bed910。58 skills/562 files 与 generated Pi bundle 已同步，retired aili-task-board schema 未被重新引入；bundle 只加载 Agent-selection/package-envelope 协议，doctor 检查 formal notes reference。typecheck 通过，9 个聚焦文件 84 tests passed，全部生成/证据 verifier 通过。
[2026-08-24] 本机 rose-aili 从 0.4.7 更新到 0.4.8，并以 pi profile 刷新全局上下文/skills/prompts；冲突的旧安装均由 installer 备份。aili-pi 本地路径已在 pi list 中注册，npm run build:web 成功生成 dist/web（438 个源文件，Pi 0.84.2）；Next 对动态依赖给出一条既有 warning，但构建、TypeScript、静态页面生成和 trace 收集均成功。
[2026-08-27] 用户批准现代化恢复异步调度外围：保留当前 sub + managed/herdr backend 与 task_id 机制，不回退旧 Subagent 实现；重新公开 top-level background:true，恢复 hub jobs/wait/output/history/send/cancel 与自动投递；foreground 仍默认；不恢复 formal_task、旧 run/attempt selector或旧模型fallback。授权当前 dirty main 的相关 harness/runtime/docs/tests编辑，不含依赖、Git、提交或推送。
[2026-08-27] 异步协调恢复完成：SUB_TOOL_SCHEMA/validateSubRequest 重新接受 top-level background，nested 继续 SUB_BACKGROUND_NESTED；runtime 注册轻量 hub jobs/wait/output/history/send/cancel，wait 默认无超时、显式 timeout_ms/AbortSignal 才终止，send 复用当前 sub/task_id 并后台执行，output/history 复用 bounded+redacted readers。README/docs/doctor/runtime prompts 更新；公开 background 自动投递测试、hub jobs/wait/output 集成测试通过。typecheck、45 个 runtime 聚焦测试、doctor 16、adapter evidence/compatibility、OpenSpec strict 均通过。
