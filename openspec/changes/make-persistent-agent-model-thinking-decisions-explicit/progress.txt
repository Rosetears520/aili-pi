[2026-08-21T20:20:00+08:00] BUILD make-persistent-agent-model-thinking-decisions-explicit EXECUTED (task path + hub model)
evidence=verification:BUILD-model-thinking-explicit-v1
disposition=accepted-with-one-deferral
blocker=implemented per design.md D1/D2 plus hub model thinking (D3 first half): captureTaskModelRequest now returns absent|captured|rejected(+reason) with no silent catch (exported for tests); production preallocate applies explicit/delegated authority-validated requests directly as the new direct-user-turn top layer (no re-confirmation), routes inherit-only requests — including thinking-only, previously dropped — through one fresh Parent confirmation, records deny/dismiss/headless as rejected-unauthorized with a distinguishing reason, and reclassifies the unusable-request fallback as rejected-unsupported; SubagentModelDecision {requestedModel, requestedThinking, overrideDecision, reason} flows through TaskPreflightResult/CreatedTask into the async accepted result, job/turn metadata (overrideDecision/modelRequestReason), and the executor turn.audit. model-selection: ModelLayer/ModelChoiceSource gain direct-user-turn; resolveModelChoice now resolves model and thinking per field (each takes the first layer providing it; profile thinking participates only at its fallback rank), the bare-model gate covers direct-user-turn, oneShot/directUserTurn inputs are TaskModelRequest-typed, and confirmTaskModelRequest confirms thinking-only requests with a dedicated prompt summary. hub model action accepts a thinking literal (validated against MODEL_THINKING_LEVELS, rides with the required model into ModelConfigurationService requests). Deferral recorded in drift-log.md: hub send turn-scoped model/thinking one-shot needs a LiveAgentAdapter protocol extension and is postponed — strictKeys still rejects the fields, no half-implementation. Tests: model-selection gains the direct-user-turn layering case (instance model + one-shot thinking per field) and the instance-precedence assertion updated to the per-field spec; model-authority gains four structured-capture cases; confirmTaskModelRequest gains the thinking-only path; 80/80 across the seven persistent-agent suites incl. formal orchestration; adapter-evidence subagent.dispatch hash refreshed for production.ts (validate:compatibility 40 records); typecheck clean; openspec validate --strict valid; full npm test green except the pre-existing pi-0821 full-load flake
next_action=proceed to separate-formal-task-adapter-from-persistent-task (stage 4)

[2026-08-28] DEFINE DELTA recorded from explicit user direction: a current user message that names the model and/or thinking for current-turn subagents must create exact value/scope-bounded `explicit` authority. Managed and Herdr share the same resolution; Herdr is not the restriction source; model-generated tool arguments cannot self-authorize; unavailable or incompatible requests fail strictly. Proposal/design/spec/tasks and a draft test-plan were updated. No runtime code changed; tasks 3.3 and 4.3 remain pending until the revised test plan is explicitly accepted and BUILD is authorized.
[2026-08-28] User accepted the revised test-plan and authorized BUILD. Tasks 3.3/4.3 implemented: exact Parent prompt authority survives first-turn sessions without a materialized path via stable session identity, selector/value scope is enforced, authority clears on Parent settlement, managed/Herdr consume the same preallocated choice, unavailable/incompatible requests fail strictly, and model-alias matching no longer treats a shorter ID as a prefix of an extended ID. Focused model/authority/backend/production suites 49/49 PASS; `npm run typecheck` PASS. Runtime reload is required before the current Pi session can use this new behavior. Deferred hub-send task 3.2 remains open.
[2026-08-28] REVIEW REPAIR aligned implementation with the accepted field order: direct-user-turn > instance > project-role > user-role > confirmed one-shot > parent > profile > runtime. Inherit-only model-generated proposals that are malformed/unavailable/denied/no-UI/incompatible now continue configured/Parent dispatch with explicit rejected audit decisions; exact current-user explicit/delegated requests remain strict and fail without fallback. Focused authority/selection/backend/production tests 49/49 PASS and typecheck PASS.
